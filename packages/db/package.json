{
  "name": "@postcard-platform/db",
  "version": "0.0.0",
  "private": true,
  "main": "./client.ts",
  "types": "./client.ts",
  "scripts": {
    "build": "echo 'Source package - Prisma client generated via postinstall'",
    "db:generate": "prisma generate --schema=./prisma/schema.prisma",
    "db:push": "prisma db push --schema=./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@supabase/supabase-js": "^2.106.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
